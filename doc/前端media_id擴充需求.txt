你是個前端工程師，替我擴充某個前端專案的功能
擴充頁籤名稱：id="nav-video"

## 現況：
- 我僅能透過 crtl + F 搜尋
- 手風琴是設計成三階段式載入
	- 第一階段(初始進入網頁)：只會載入所有網紅的基本資訊
	- 第二階段(點擊指定網紅)：會展開該網紅的所有影片基本資訊
	- 第三階段(點擊指定影片)：才會展開該影片的詳細資訊


## 需求
- 在這個頁籤功能中，我想在 nav 以下、影片展示手風琴以上 (第一行是蔡阿嘎) 的地方，新增一個 "條件搜尋" 的 section
- 階段1 (較簡單)：media_id 搜尋
	- 初始網頁載入時維持原本的樣子 (可看見200個網紅未展開的基本資訊)
	- 當我 input 某個 media_id，按下【搜尋】後，會自動展開所屬網紅的該影片，並 zoom 到那個位置
	- 例如：當我 input "1225445875663624"，會自動 zoom 到網紅 2uncle987 的位置，並展開 1225445875663624 這部影片 (也要移動到可看見 1225445875663624 的位置)
- 階段2 (條件式篩選)
	- 初始網頁載入時維持原本的樣子 (可看見200個網紅未展開的基本資訊)
	- 當我執行條件篩選後，只會剩下符合條件的影片以及他們所屬網紅的容器 (但不用展開網紅的影片基本資訊，維持在網紅層級就好)
		- 如果我進一步展開網紅手風琴，只會看到符合篩選條件的影片 
	- 需要有一個【篩選】
	- 需要有一個【重設】按鈕，點擊後恢復初始狀態  (可看見200個網紅未展開的基本資訊)
	- 條件1：依照影片長度 (duration) 篩選 (min, max)
	- 條件2：依照影片留言數量 (statistics.comment_count) 篩選 (min, max)
	- 條件3：依照影片按讚數量(statistics.like_count) 篩選 (min, max)
	- 條件4：依照影片建立時間(creation_time_tw)篩選 (start, end)
	- 條件5：依照影片修改時間(modified_time_tw)篩選 (start, end)
	- 以上條件 1 ~ 5 應建立 radio button，一次只能夠選一種條件篩選
	- min, max 的元件與使用方法：分為兩個 number 元件
		- 如果只在 min 輸入，就找大於 min 的時間的影片；如果只在 max 輸入，就找小於 max 的時間的影片
		- 如果同時在 min、max 都輸入，就找介於這個範圍內的時間的影片
	- start, end 的元件與使用方法：分為兩個日期選擇元件
		- 如果只在 start 輸入，就找大於 start 的日期的影片；如果只在 end 輸入，就找小於 end 的日期的影片
		- 如果同時在 start、end 都輸入，就找介於這個範圍內的日期的影片



## 檔案說明
- 我已在童專案"input\all_infleuncer_media_id.csv"這個路徑新增了一個 `all_infleuncer_media_id.csv` 檔案，裡面有條件搜尋所需目標
- 其餘的程式碼我也提供給你
- 只能異動我有提需求的地方，其餘地方嚴格維持原本的